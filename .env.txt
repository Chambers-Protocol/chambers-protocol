# ==============================================
# THE CHAMBERS PROTOCOL - ENVIRONMENT SECRETS
# ==============================================
# WARNING: NEVER commit this file to GitHub.
# Add '.env' to your .gitignore immediately.

# 1. CENTRAL LEDGER CONNECTION (The Revenue Link)
# This is where your server sends the "Tax" data.
# For local testing, you can leave these as placeholders or point to localhost.
CENTRAL_LEDGER_URL="https://your-supabase-project.supabase.co/functions/v1/capture-tax"
CENTRAL_LEDGER_SECRET="your-service-role-secret-key-goes-here"

# 2. NODE CONFIGURATION
# Defines how the server identifies itself to the network.
NODE_ENV="development" # Change to 'production' when deploying
LOG_LEVEL="INFO"       # Set to 'DEBUG' to see raw tax transmission logs

# 3. LOCAL OVERRIDE KEYS
# Keys that allow YOU (the admin) to bypass payment for testing.
CHAMBERS_ADMIN_KEY="chambers-admin-override-key-8829"

# 4. OPTIONAL: AI PROVIDER KEYS
# If your node needs to make independent AI calls (rare, usually the Host does this).
# OPENAI_API_KEY="sk-..."
# ANTHROPIC_API_KEY="sk-ant-..."